name: update tide data
on:
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch:
jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - name: PHP Runner
        uses: franzliedke/gh-action-php@0.3.0
      - run: php8.1 import.php
